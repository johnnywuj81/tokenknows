import { Placeholder } from '@/components/shared/Placeholder'

export default function RedactionPage() {
  return <Placeholder task="T10 · 脱敏确认" taskFile="T10-redaction.md" description="异步 scan + polling + 分组确认/豁免。" />
}
