import { Placeholder } from '@/components/shared/Placeholder'

export default function DocumentPage() {
  return <Placeholder task="T06 · 文档生成结果页 (核心)" taskFile="T06-document-page.md" description="TipTap 编辑器 + 自动保存 + 章节级重生成 + 证据链。最复杂屏。" />
}
